#Make a matrix and add 1 to any line that has a wire
#When adding the second wire, check if that location == 2. If so, add it to the list of coordinates.
#Find the MAnhattan Distance of each coordinate to the origin
#select the shortest one.




#Step 3, find the Manhattan Distance of each coordinate.
def manhattan(x, y):
  return abs(x) + abs(y)
